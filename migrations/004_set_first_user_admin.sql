-- Set the first user (by email or ID) as admin
-- Replace 'your-email@example.com' with your actual email
-- Or use: UPDATE users SET is_admin = 1 WHERE id = 'your-user-id';

-- Example: Set user by email
-- UPDATE users SET is_admin = 1 WHERE email = 'your-email@example.com';

-- For now, this is a placeholder - run manually with your email
-- You can also do this via the database directly

